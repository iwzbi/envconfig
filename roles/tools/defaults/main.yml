---
# Centralized versions for binary-download tools.
# Upgrade: change the version number here, the download/extract/install
# tasks use version-specific temp paths so the change propagates automatically.
delta_version: "0.19.2"
zoxide_version: "0.10.0"
dust_version: "1.2.5"
lazydocker_version: "0.25.2"
